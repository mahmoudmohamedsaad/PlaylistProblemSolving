//need to convert array reverse num
//convert this num to string
//convert this string to array
// convert this array to nums
//reverse it

function digitize(n){
    return n.
    toString()
    .split("")
    .map((m)=>Number(m))
    .reverse();
}
console.log(digitize(23456789));